# IA Change Log - Sincronização context.yaml x context-index

## 1. Contexto consumido

- `context.yaml`
- `context-index.yaml`
- `code-brain/to-do/tasks.yaml`
- `code-brain/guidelines/git/workflow-template.md`
- Logs recentes (`202511081530`, `202511081531`, `202511081532`, `20251108090916`, `20251108092447`, `20251108093044`)

## 2. Objetivo da alteração

* resumo (4-6 linhas):
  Executar a T-0001 iniciada, garantindo que o `context.yaml` reflita fielmente as exigências estruturais do `context-index.yaml` para o MVP-1. Isso inclui registrar o estado real das pastas/arquivos obrigatórios, atualizar a ordem de leitura, alinhar governança e destacar lacunas (como o backlog removido). Também foi necessário materializar o diretório `code-brain/git/` e o `workflow-template` apontado pelo index.
* escopo (inclui/exclui):
  - Inclui: criação de `code-brain/git/workflow-template.md`, atualização do `context.yaml`, validação YAML e documentação.
  - Exclui: restauração do backlog anatômico, execução das tarefas subsequentes (T-0002+), ajustes no project-guide.

## 3. Decisões e justificativas

* alternativas:
  1. Apenas documentar a ausência de `code-brain/git/` em context.yaml – rejeitado pois o index exige a estrutura presente.
  2. Recriar o backlog anatômico sem insumo atual – rejeitado para evitar divergências e por falta de instrução explícita.
  3. Criar `code-brain/git/` com o workflow template e registrar lacunas remanescentes no contexto – adotado.
* decisão:
  - Materializar o diretório Git mínimo e sincronizar o `context.yaml` com listas de conformidade (diretórios/arquivos, governança) e leitura atualizada.
* riscos/limitações (com mitigação):
  - Duplicidade do workflow template (guidelines x git): mitigado adicionando nota de fonte primária no novo arquivo.
  - Backlog ausente ainda bloqueia conformidade total: destacado em `structure_alignment.files` e nos `pending_validations` para ação futura.

## 4. Implementação

* arquivos gerados:
  - `code-brain/git/workflow-template.md`
  - `code-brain/developer/logs-ai-code/20251108093426-ia-code.md`
* arquivos alterados:
  - `context.yaml`
* notas de implementação:
  - `context.yaml` agora inicia com `---`, traz ordem de leitura incluindo `prompt.md` e `context-index.yaml`, replica o `file_ownership` do index, adiciona seção `structure_alignment` com status de cada item obrigatório e atualiza a lista de validações pendentes.
  - `code-brain/git/workflow-template.md` replica o fluxo existente em guidelines e referencia a fonte original para evitar drift.

## 5. Revisão humana

* dúvidas/trechos críticos:
  - Confirmar se o backlog anatômico será recriado ou substituído por outro artefato oficial.
  - Validar se a duplicação temporária do workflow template atende às expectativas.

## 6. Follow-ups

* tasks abertas/atualizadas:
  - T-0001 continua em andamento até que o backlog seja restaurado/decidido e demais gaps sejam resolvidos.
  - T-0002 depende da conclusão de T-0001 (ainda pendente).

## 7. Next Steps

* Decidir e restaurar o `backlog_anatomico_unificado_padronizado.md` ou atualizar o context-index para refletir o artefato atual.
* Prosseguir com T-0001 validando também métricas/mvp_tracking vs project-guide.
* Assim que T-0001 estiver satisfatória, iniciar T-0002 (mapeamento do pipeline cognitivo) com base nesse contexto sincronizado.
